# SQLSpread




## Development
### ToDo


### Files
- `index.html` – HTML-Shell
- `styles/main.css` – Styles
- `sqlspread-app.js` – Bootstrap / global Hotkeys
- `state.js` – central UI- und runningstate
- `db.js` – load/save/SQLite
- `components/` – small UI elements
- `logic/` – sort, filter, edit, resize
- `render/` – tab- und Grid-rendering

